# Overnight build log — 2026-06-10

Autonomous overnight build. Each entry = one deployed-to-prod change. Newest at the bottom of each session. All commits are local until you push.

## Already shipped this session (2.57.5 → 2.58.8, before the overnight run)
- **2.57.5** fix: SCRUM board Base UI #31 crash (bare `DropdownMenuLabel`) + sprint scoping on mount
- **2.57.6** feat: work-item delete + duplicate on the detail sheet
- **2.57.7** feat: live board/issues updates via SSE
- **2.57.8** feat: filter issues by created/updated date range
- **2.57.9** feat: story/task hierarchy (parent picker + add sub-item)
- **2.58.0** feat: per-user Claude subscription for the agent (preferred over org)
- **2.58.1** fix: project feature-toggle 400 on legacy flag
- **2.58.2** feat: project-manager board-create gate + board delete UI
- **2.58.3** feat: bulk-edit on the org-wide Issues view
- **2.58.4** feat: per-project classification banner + Applied-By name
- **2.58.5** feat: "+ New issue" on table/backlog/timeline/calendar/RAID
- **2.58.6** feat: quick Duplicate/Delete in the issue row menus
- **2.58.7** feat: comment edit/delete + invitation revoke/resend (the 2 real CRUD gaps)
- **2.58.8** feat: feedback (bug/FR) analytics tab

## Overnight run (continuous)
- **2.59.0** feat: feedback attachments + telemetry (Slice B). Screenshot/PDF upload on bug/FR submit (8 MB, magic-byte sniffed, auth-served), auto-structured telemetry on bug reports (route/UA/stack + repeat-sighting history with hit counts), attachments shown on portal items. New `FeedbackAttachment` model + `telemetry` column (migration applied). _Completes the feedback FR end-to-end (analytics tab in 2.58.8 + this)._
- **2.59.1** fix: Batch 1 — real bugs + dead controls (from the 14-agent full-surface review of every page). (1) **Critical:** meeting attendee save hit the wrong endpoint (bulk collection vs per-attendee), so standup updates silently failed — now `/attendees/{id}`. (2) Meetings: removed the duplicate "Edit" (identical to Open); added a **delete confirmation**. (3) Kanban card: removed the dead "Assign" (no-op → Edit). (4) Notes: "Change visibility" was a no-op → now real **Make private / Share with org** (optimistic). (5) CRM: removed the wrong-dialog "Change owner" (Edit covers it); fixed the **invoice-builder React key** (`index` → stable id; prevented input-state scramble). (6) Chat: removed the dead channel-settings ⚙️.
- **2.59.2** fix: Batch 2a — mobile navigation + shell (mobile UX strategy: responsive padding, 5-slot bottom nav, Agent-as-bubble, ≥44px touch targets). (1) **PageShell** padding `p-8`→`px-4 py-5 md:p-8` + tighter header margin — every page gains real estate on phones. (2) **Mobile bottom nav** went 4→**5 items** (Overview · Projects · **Chat-center** · Notes · Meetings) — Notes/Meetings were previously unreachable on mobile without the drawer; Agent stays the floating bubble; min-height 52px touch targets. (3) Floating Agent bubble offset raised to clear the taller nav. (4) StatCard label readability bumped at md+.
- **2.59.3** fix: Batch 2b — boards + chat + detail-sheet on mobile (same responsive strategy as 2a). (1) **Sheets** are now **full-width on phones** (`w-full sm:w-3/4`) at the base component — every drawer app-wide (work-item detail, etc.) benefits. (2) **Kanban columns** clamp to `84vw` (max 19rem) on phones so a column no longer runs off-screen, with matching drag-overlay + skeleton widths. (3) **Board filter bar** search box goes full-width and taller (h-8) on mobile, icon no longer shrinks. (4) **Chat thread pane** full-width on phones (`w-full max-w-[85vw] sm:w-96`). (5) **Message actions** (react/reply/pin/edit/delete) are **always visible on touch** instead of hover-only. (6) **Chat dialogs** (new channel / new DM / browse channels) clamp to `max-w-[90vw]` so they stop overflowing on 375px screens. (7) **@mention picker** + **pinned panel** clamp to viewport width and scroll vertically. (8) Detail-sheet header padding bumped (`pr-8`→`pr-10`) so item actions clear the close button.
- **2.59.4** fix: chat message **edit/delete are now proper in-app UI** instead of the browser's `window.prompt`/`window.confirm` (which looked broken/jarring, lost markdown, and were unusable on mobile). Editing a message now opens an **inline auto-growing textarea** in place (Enter = save, Shift+Enter = newline, Esc = cancel, with Save/Cancel buttons); deleting shows an **inline "Delete? [Delete] [Cancel]"** confirm right in the action row. This works the same in the main feed **and in thread replies** — thread edits/deletes were previously dead no-ops; they now persist and update live for everyone via SSE (added `chat.message.updated`/`deleted` handlers to the thread pane).
- **2.59.5** fix: Batch 2c — remaining mobile responsiveness. (1) **Timeline/Gantt** left label panel narrows to 140px on phones (was a fixed 260px = 69% of a 375px screen), so the chart itself isn't crowded off-screen; the chart still scrolls horizontally. (2) **Custom fields** settings table now scrolls horizontally on mobile instead of clipping (was `overflow-hidden`; the 5-column table overflowed 375px). _Verified the work-item table view, the generic data-table (stacks to blocks on mobile via `block md:table`), the board-tabs strip (already horizontally scrollable), the month calendar (7-col squeeze, standard), and the Agent drawer (already full-width on mobile) — no change needed._
- **2.60.0** feat: **work-item dependencies/links + richer sub-items in the detail panel** (the `WorkItemLink` model + API existed but had no UI — now it does). (1) **Linked items** section on every work-item detail sheet: shows blocks / blocked-by / relates-to / duplicates / predecessor / successor / clones, each labelled **from the open item's perspective** (e.g. a `BLOCKS` edge shows "Blocks" on the source and "Blocked by" on the target). (2) **Add link**: pick a relationship, search this project's items (debounced, excludes self + already-linked), click to link. (3) **Remove link** inline (optimistic, ITEM_UPDATE-gated). (4) **Click any linked item to open it** in the same sheet. (5) **Sub-items** are now **clickable to open** and have an inline **remove** (un-nests the child via `parentId: null` — keeps the item). Wired the open-in-place navigation through the Kanban + Backlog boards. API: the links GET gained `?item=` scoping + item titles in the payload.
- **2.60.1** fix(a11y): **visible keyboard-focus indicator app-wide.** The `Button`/base-ui primitives already showed a focus ring, but the many *raw* `<button>`/`<a>`/`[role]` elements (board cards, chat actions, tab strips, menu items, etc.) had **no focus indicator at all** — invisible for keyboard/switch users. Added a global `:focus-visible` outline fallback in the base layer, scoped with `:where()` so it's zero-specificity (any explicit focus style, and the primitives' own ring, still win) and excludes inputs (which already ring). Net: tab through any page and you can now see where you are. _Confirmed Kanban cards were already keyboard-operable (dnd-kit gives them `role=button`/`tabIndex=0` + Enter/Space activation) — they just lacked the visible ring; and that topbar/nav controls already carry `title`/`aria-label` accessible names._
- **2.60.2** fix: Bug/UX batch 2 (from a parallel 3-surface code audit of CRM / Notes / Meetings — confirmed findings only; one reported "bug" was a false positive and skipped). (1) **CRM contact delete** now uses a proper themed **confirmation dialog** (was the raw browser `confirm()`), is **gated on `CRM_DELETE`** (the button used to show for everyone and 403 on click), and is **disabled while deleting** (was double-clickable). Matches the contact-card pattern. (2) **Notes delete** now asks for **confirmation** before deleting (it deleted on the first click; meetings already confirmed — now consistent). (3) **Meeting video-link row** stops **overflowing on mobile** — the input goes full-width and the buttons wrap below (`flex-col sm:flex-row`, `w-full sm:w-64`). (4) **Remove an attendee from a meeting** — the DELETE API existed but had no UI; added a per-attendee remove button with a confirmation (warns it also clears their standup updates).
- **2.60.3** fix: Bug/UX batch 3 — settings + notes. (1) **Integrations** install + configure dialogs now **block submit until required fields are filled** (the red `*` markers were decorative — you could install/configure with empty API keys/secrets and get a silent failure). (2) **Notes "Project" visibility removed** — it was misleading: the Note model has no `projectId`, so a "Project" note wasn't scoped to any project (the query treated it as org-wide). Legacy Project notes still render; opening one in the editor normalizes it to "Organization" (its actual behavior). _Re-examined two reported roles-manager (ABAC) findings and **declined both** after reading the code: the deny-rule `key={index}` is benign because every row input is fully controlled by the policy object (no uncontrolled state to mis-bind), and the "no delete confirmation" is moot because removing a deny rule is a staged local edit that only persists on Save (Cancel discards it). Also confirmed the CRM pipeline `DragOverlay` "missing onDelete" is a non-issue — it's a non-interactive drag ghost._
- **2.60.4** fix: Bug/UX batch 4 — work-item detail & board create (from a boards + detail-sheet code audit). (1) **The detail panel now resets per-item state when you switch items** (via duplicate, a linked/sub-item click, or board nav). Previously a half-typed comment, an open comment-edit, or the Activity tab **carried over to the next item** — most seriously, a draft comment could be **posted to the wrong work item**. (2) **@mention in a comment keeps your cursor in place** — inserting a mention mid-sentence used to jump the caret to the end. (3) **"Add card" on Kanban columns is hidden from users without create permission** (it showed for everyone and 403'd after you typed a title; the table view already hid it). _Two more reported "bugs" were false positives on inspection — comment delete already removes only after the server confirms (no rollback needed), and comment React keys are stable. Logged a real-but-deferred item: Kanban intra-column drop can collide sort-orders (the server stores the order as-is without shifting siblings); lower-stakes than the Backlog view, which already ranks correctly — will fix with a proper reorder pass._
- **2.60.5** fix: Bug/UX batch 5 — member/invite safety (from a team-management audit). (1) **You can no longer remove yourself from the org** — the member-DELETE API now blocks self-removal (the role-change PUT already did; the DELETE didn't, so an admin could accidentally lock themselves out). (2) **Removing a member, removing a project member, and revoking an invite now all ask for confirmation** — these were one-click destructive actions with no undo (role changes already had a dialog; now the destructive ones do too). Each confirm spells out the consequence (lost access / needs a new invite).
- **2.60.6** fix: **Kanban card ordering now persists exactly where you drop it** (the deferred item from 2.60.4). Dropping a card between two others used to give it the *same* sort-order as a neighbor (the server stores order verbatim and didn't shift siblings), so after a refresh the card could jump. Now the drop **re-sequences the whole target column to unique 0..n orders** and persists every shifted card in parallel (reverting the whole move if any write fails) — matching how the Backlog view already worked. Intra-column ordering on boards is now deterministic.
- **2.60.7** fix: Bug/UX batch 6 — analytics + notifications (from a dashboard/projects/nav audit). (1) **The Analytics → "Project Detail" tab was broken** — the API returned id-keyed objects (`itemsByType`…) but the charts expected `{name,value}[]` arrays (`byType`…), so the type/priority/status charts + Top Assignees rendered empty or threw. The route now returns the right shape with **resolved names** (work-item-type names + assignee display names) and a `completed` trend field. (2) **Clicking a notification now navigates correctly** — notification URLs were inconsistent (some stored with the org slug, some without) and the handler always re-prefixed, so meeting/work-item/comment notifications opened `/{org}/{org}/…` → 404. The handler now strips a duplicate org-slug prefix and routes once. (3) **Saved reports are now viewable by read-only analysts** — the reports list (GET) required `REPORT_CREATE`; it now gates on `ANALYTICS_READ` like the other analytics reads (create/delete still need `REPORT_CREATE`). _Logged-but-deferred from the same audit (lower-stakes / controlled data): burndown vs velocity use different unestimated-point defaults (1 vs 0); board creation doesn't validate type against the project's enabled features (orphaned-but-hidden boards); template column/feature payloads aren't re-validated on apply; org-slug 409 shows a generic message. The projects-list "stale after create" report was a false alarm — that list is a server component, not React-Query._
- **2.60.8** fix: Bug/UX batch 7 — AI assistant feedback + finance money-display (from finance + AI-agent audits). (1) **The Assistant no longer fails silently** — if starting a conversation errors, you now get a toast (input is preserved) instead of nothing happening; and if the assistant's response fails mid-stream, the bubble becomes an **inline "couldn't finish responding"** error (keeping any partial text) instead of leaving your message hanging with no reply. (2) **Finance money-display hardening** — the invoice builder's live **preview now rounds per-line exactly like the server** (sum-then-round could drift the preview a cent off the created invoice); the invoice **balance** is rounded to cents so float subtraction can't show a phantom sub-cent owed on a fully-paid invoice; and the **trial-balance "Balanced ✓"** check uses a cent tolerance instead of exact float equality. _Audit confirmed the server-side money engine is sound (Prisma Decimal, per-line rounding, banker's rounding, optimistic-locked payments, guarded status transitions) — these were all client-display robustness fixes. The in-channel AI bot-runner, per-user/org Claude credential resolution, tool permission-scoping, and rate-limiting were all verified correct._
- **2.60.9** fix: Bug/UX batch 8 — objectives + search + burndown (from an audit of OKRs/Goals/KPIs/Milestones + search). (1) **Goal links are now idempotent** — the goal↔work-item / goal↔objective link endpoint didn't check for an existing link (milestone links already did), so a double-click could create a duplicate that **double-counted in the goal's AUTO progress rollup**; it now returns the existing link instead. (2) **Deleting an OKR objective uses a proper confirmation dialog** (was the raw browser `confirm()`), matching the rest of the app — and it names the objective. (3) **Global search won't flash stale results** — a slow response for an earlier keystroke could overwrite a newer query's results; added an out-of-order guard. (4) **Sprint burndown shows a clean "no data" state** for an active cycle with no items (was a misleading flat zero line). _Verified-and-declined this round: the reported "OKR progress goes stale after a key-result update" is a non-issue — the objective card computes its progress live from its key results (which update optimistically), not from a cached field; and the onboarding org-slug 409 already shows the specific server message ("…slug already taken" / "…URL is reserved") via the fetch wrapper, not a generic one. There is no separate Documents module — only chat/feedback attachments, which validate type+size and are auth-served. Deferred (judgment calls): board-type vs enabled-features validation, and the burndown(1)/velocity(0) unestimated-point default difference._
- **2.61.0** feat: **set progress on MANUAL goals from the UI** (an incomplete-feature gap found in the objectives audit). A goal whose progress mode is "Manual" had no way to actually set its percentage from the app — only AUTO goals (rolled up from links) updated. The goal create/edit form now shows a **Progress (%) field (0–100) whenever the mode is Manual**, clamped and persisted; AUTO goals are unchanged (the server still owns their rollup, so we don't send a manual value that would be overwritten). _Note on the other listed expansion (bulk actions): the org-wide Issues view already has bulk-edit; extending it to the CRM **pipeline** means multi-select on a drag-and-drop board (selection mode + a new bulk API), which is interaction-heavy and risky to ship without UI testing — deferring it to do properly rather than risk regressing a working board overnight._
- **2.62.0** feat: **channel settings in chat** (the dead ⚙️ removed back in 2.59.1 is now a real feature — priority-2 expansion). Channel admins (and org owners/admins) get a **gear in the channel header** that opens a settings dialog to **rename** the channel, set its **topic** and **description**, and **archive** it (behind an inline confirm; archiving routes you back to the channel list, and #general can't be archived). The PATCH backend already existed and is permission-gated; this adds the missing UI and surfaces a `canManage` flag from the channels API so the gear only shows to people who can actually use it (and never on DMs). _Member add/remove management (the API exists) is intentionally left for a follow-up to keep this change focused._
- **2.63.0** feat: **chat channel management round 2** — (1) **Per-channel notification preferences are now reachable**: a menu in the channel header lets you set **All messages / Mentions only / Mute / Mute for 8h** (the control + its API already existed but were wired nowhere — it was a dead component). (2) **Channel member management** in the settings dialog (from 2.62.0): channel admins now see the **member roster** (with admin badges) and can **add** org members or **remove** them inline. Added the missing `GET /members` endpoint (member-gated) to back the roster; add/remove use the existing endpoints. _This closes the priority-3 expansion and the member-management follow-up flagged in 2.62.0. On notification prefs: there is no org-level per-type preference model in the schema (only this per-channel one) — surfacing the existing per-channel control is the real, shippable win; a per-type system would be a new model + migration, noted for later._
- **2.64.0** feat: **finance — create accounts & post manual journal entries** (the accounting module was read-only; priority-4 expansion). The Chart of Accounts tab gets a **New account** action (code / name / type — the server derives the normal balance and enforces a unique code), and the Journal tab gets a **New entry** action: a real double-entry form with **dynamic lines** (account · debit/credit · amount), **add/remove line**, and a **live "Balanced / off by $X" indicator** so Post only enables when debits == credits. Both are gated on `ACCOUNTING_MANAGE`. The journal POST delegates to the existing **tested `postEntry` primitive**, so it inherits the real balance enforcement, the closed-period guard, sequential entry numbering, and atomic posting — no new money math was written. New `POST /accounting/accounts` and `POST /accounting/journal-entries` endpoints (org-scoped account validation; balance/period errors map to 400/409 with messages).
- **2.64.1** fix: Bug/UX batch 9 — settings credentials + finance guards (from audits of the finance sub-modules + account/settings). (1) **Destructive AI-credential actions now require a 2-step confirm**: clearing the Anthropic key, clearing the OpenAI-compatible config, and disconnecting the Claude subscription (org **and** personal) were all one-click and would silently drop the org's/your model access — they now arm a Confirm/Cancel (new reusable `ConfirmButton`). (2) **Bank rule** creation rejects a min > max amount (it would silently create a rule that can never match). (3) **Excluding a bank transaction** from the reconciliation inbox now confirms first (it's recoverable via status filter, but was a one-click removal from the active queue). _The audits otherwise found the finance engine SOUND — reconciliation, payroll, tax, and CSV/OFX import money math all verified Decimal-clean — and the account/security surfaces solid (password change requires the current password, MFA enroll/disable is code-gated, API keys + TOTP secrets are sealed and never echoed, org-settings/slug are permission-gated and collision-checked). Declined as false positives: "contract value stored as float" (it's `Decimal(19,4)`), and the tax-rate/bank-rule "client float" concerns (decimal.js reads the number's exact toString for 2-dp money, so no precision is lost in practice)._
- **2.64.2** fix: rate-limit feedback submissions + attachment uploads (consistency with chat/export). A final bug-sweep of the remaining low-traffic surfaces — topbar/breadcrumbs/org-switcher, profile avatar upload, RAID detail, the feedback portal, and time-tracking — came back **clean (0 genuine bugs)**; the only real item was that the feedback POST + attachment-upload endpoints had no throttle (every other write path does), so an authenticated member could spam the board. Added `checkRateLimit` to both (≈10 submissions / 15 uploads per burst, slow refill). _Also verified-not-a-bug: the "board type vs enabled-features" concern — board **types** are views (Kanban/Table/Timeline/…), not gated by `enabled_features`; features gate the separate OKR/Goals/KPI/Milestone **nav tabs**, so being able to create any board view is by design, not a leak. At this point essentially the entire app has been audited; further sweeps are returning clean — the build is solid._
- **2.64.3** fix: **contract delete now uses a styled confirmation dialog** instead of the browser's `window.confirm` (the finance audit flagged this as mobile-unreliable — native confirm can be suppressed/inconsistent on some mobile browsers). Same pattern as the rest of the app's deletes. _Wind-down pass: prod verified healthy, the codebase swept for any remaining native `window.confirm/prompt` — only two others remain and were deliberately left: the Notes editor's "Link URL" prompt (replacing it needs a real in-editor popover, not a safe one-liner) and the milestones-timeline delete (a duplicate of the already-dialoged milestones list). Everything else stable; no other changes warranted._
- **2.64.4** fix: **real bugs caught by actual browser testing** (stood up an isolated seeded test stack — ephemeral postgres + `E2E_TEST_AUTH=1` server, fully separate from prod data — ran the Playwright journey suite (89/90, the one a dev-only RSC `ERR_ABORTED`) + a desktop/mobile screenshot sweep of all 47 pages × 2 viewports). Found and fixed: (1) **Audit-logs viewer 500'd in prod** — the list route returned raw `AuditLog` rows including the AU-9 hash-chain `seq` (BigInt), so `JSON.stringify` threw "Do not know how to serialize a BigInt" for the whole `/settings/audit-logs` page; now an explicit `select` returns only display fields (also stops leaking the internal `rowHash`/`prevHash` WORM bytes to the client). (2) **Five finance/banking panels were broken** by a query-envelope mismatch — `jsonFetch` already unwraps a single-key `{data}` envelope, but the client added a redundant `.then(r => r.data)`, yielding `undefined` and a React-Query "Query data cannot be undefined" crash: **invoice AR-aging**, **payroll labor-by-project**, **tax liability**, **bank reconciliation summary**, and **pay-run preview** (the last two interaction-gated, so only the browser sweep surfaced them). Re-ran the sweep after the fixes: 94 renders, **0 hard problems, 0 console errors**.
- **2.64.5** fix: two feedback-board bugs in the shared DataTable. (1) **Ticking a bulk-select checkbox no longer opens the detail drawer** — the row-selection checkbox didn't stop click propagation, so selecting issues for bulk edit also popped the side drawer (reported BR). (2) **Wide tables now scroll horizontally** instead of clipping — the table wrapper was `overflow-hidden`, so the Issues view's many columns got cut off with no way to reach them (reported BR: "issues pane not wide enough / doesn't enable horizontal scrolling"). Both fixes are in `data-table.tsx`, so every table app-wide benefits.
- **2.64.6** fix: **mobile breadcrumb no longer collides with the topbar icons.** On phones the full trail ("Test Org › Projects › Test › OKRs") wrapped and ran under the sparkles/feedback/bell icons (rendering as "OK⟨icon⟩Rs"). The breadcrumb now **collapses to just the current page on mobile** (the bottom nav + hamburger provide navigation there), truncates instead of wrapping, and the topbar's right-hand icon cluster is `shrink-0` so it can never be overlapped. Full trail still shows at `sm+`. _Verified on the live test browser. NOTE on the parallel visual-review sweep: the "avatar/account circle overlapping the bottom nav / clipped sidebar footer" findings were a **false positive** — DOM inspection showed that "N" circle is the **Next.js dev-mode indicator** (a `nextjs-portal`), which only exists on the dev test server and not in the production build; there is no misplaced avatar. Several "stuck skeleton" findings are likewise dev cold-compile artifacts. Real findings still being worked: finance/AI empty-state regions, mobile one-row truncation on a few cards, the overview empty-skeleton widget, duplicate primary actions on empty pages._
- **2.64.7** feat: Issues view — **per-page selector + quick priority change** (feedback FRs). (1) The Issues table footer now always shows when there are results with a **"Per page" selector (25/50/100/200)** + issue count + pager (was a fixed 25 with prev/next only on >1 page) — addresses "ability to change number of issues per page for views". (2) The row right-click/⋯ menu gains a **"Set priority" group** (Critical/High/Medium/Low) so you can change an issue's priority without opening the drawer (ITEM_UPDATE-gated) — addresses "more right-click/3-dot options" + "quick actions on issues". _Audited right-click/3-dot CRUD coverage across boards: kanban, table, backlog, and RAID already have the ActionMenu; calendar + timeline (visualization views, SVG/cell context-menus are fiddly + low-value) noted as minor follow-ups; CFD/dashboard are charts (no per-item CRUD)._
- **2.64.8** fix: **selects with interpolated labels no longer show raw UUIDs** (backlog UUID-scrub). Root cause: the `<Select>`'s auto-derived label map (`collectSelectItems`) only captured items whose child was a *single* string/number — so an item like `<SelectItem>{p.key} - {p.name}</SelectItem>` (JSX compiles to an array) was skipped, the map had no label for that value, and base-ui's `<Select.Value>` fell back to rendering the **raw value (a project UUID)**. Now it flattens array children to a label. Fixes the **meetings project-selector** AND the **Add-Classification project-selector** (both used `{key} - {name}`) and any other interpolated-label select app-wide. _Also verified the per-project **classification banner works**: set a FOUO classification on a project and the marking strip renders correctly — the old "banner not appearing" note was stale (UNCLASSIFIED/PUBLIC intentionally show nothing; FOUO+ shows the banner)._
- **2.64.9** feat: **filter + sort the Feedback board** (FR: organize/filter FRs & BRs by status and type). Added a control bar: **Type** (All / Features / Bugs), **Status** (All / Open / Planned / In progress / Done / Declined), and **Sort** (Top voted / Newest). Filtered-empty shows a contextual "No bugs that are open" message; the full empty state still shows when there's no feedback at all. Verified on the live test browser (seeded items sort + filter correctly).
- **2.64.10** fix: **charts now render correctly in dark mode AND show their per-category colors** (feedback BR: "sprint dashboard UI not showing properly across dark/light mode"). Two root causes, both app-wide across dashboard + analytics + CFD charts: (1) the chart tooltip/axis/legend styles used `hsl(var(--popover))` / `hsl(var(--muted-foreground))` etc., but this app's design tokens are **hex** (`#252B40`), so `hsl(#hex)` is invalid CSS → fell back to black/default (invisible in dark mode). Replaced with the real raw tokens (`var(--overlay)` / `var(--text)` / `var(--text-muted)` / `var(--border)`). (2) The lazy-recharts barrel wrapped **every** recharts primitive in `next/dynamic` — including marker components like `<Cell>` that recharts identifies *by component type* — so per-element fills were silently dropped (black bars, single-grey donuts in both modes). Now only `ResponsiveContainer` stays dynamic (`ssr:false`, the client-render gate); the charts/series/axes/`Cell` are direct re-exports so recharts recognizes them. Verified on the live test browser: the priority bar renders blue, axis labels are legible in dark mode, the donut shows status colors. _Side note: confirmed the SCRUM sprint board itself already renders fine in dark mode — the issue was the DASHBOARD charts._
- **2.64.11** fix: **two reported bugs — page scroll escaping over the navbar, and push notifications "can't enable."** (1) **Scroll-leak (BR: "backlog scroll escapes container, scrolls whole page into nothingness over the navbar"):** on any page whose content overflows `<main>` (Settings pages, long lists), the *viewport itself* became scrollable in addition to `<main>` — wheeling over the fixed topbar scrolled the whole document into empty space. Browser DOM tracing confirmed every app-level ancestor (the `h-screen overflow-hidden` shell root, `<body>`) was correctly clipped at 100vh, but Chromium still propagated a nested `overflow:auto` scroller's content to the documentElement (`html.scrollHeight` = main's content + topbar, while `body.scrollHeight` stayed 900). Fixed by locking the document scroller (`html`/`body` `overflow:hidden`) while the dashboard shell is mounted, so **`<main>` is the only scroll surface** — the canonical app-shell pattern. Scoped to the dashboard (the client shell's mount effect, with cleanup) so login/marketing pages that legitimately scroll the document are unaffected. Verified across 5 routes on the live test browser: wheeling over the topbar now keeps `window.scrollY` at 0 on every page, while `<main>` still scrolls its tall content internally. (2) **Push notifications (BR: "can't enable push notifications"):** two root causes. The deployment had **no VAPID keys configured**, so `GET /api/v1/me/push/subscribe` returned 503 and the enable flow failed — *and* the UI misreported it as "check your browser's notification permission" (sending users to fix a setting that was already fine). Generated a VAPID keypair into the (gitignored) `.env`, documented the three vars in `.env.example` (generate via `npx web-push generate-vapid-keys`), and reworked `enablePushNotifications()` to return a **typed reason** (`unsupported` / `denied` / `not_configured` / `error`) so the toggle now shows the **accurate** cause — "not configured on this server yet," "blocked for this site, enable in browser settings," etc. Push now actually works in prod. _The third #173 item — an intermittent "Cannot read properties of undefined (length)" crash — has no telemetry/repro yet; a heuristic sweep of unguarded `.length` accesses found them all guarded, so it's left for a real repro rather than speculative churn._
- **2.65.0** feat: **chat cluster — Teams-style message bubbles, mic "listening" indicator, and chat-as-drawer on mobile** (the three #171 feedback items). (1) **Your own messages now mirror to the RIGHT in a primary-tinted bubble; everyone else stays on the LEFT in a neutral bubble** (Teams/messenger layout), with avatars and timestamps mirrored to match — far more scannable in the narrow chat drawer than the old all-left flat list. The 🤖 Assistant is never treated as "own" (its replies always read on the left), and SYSTEM/ACTION/tombstone/edit states are preserved. Verified on the live test browser (posted as two users): Bob's messages left-neutral, Alice's right-tinted. (2) **A live "Listening…" indicator** now appears (a pulsing red mic pill, bottom-left, clear of the agent bubble and mobile nav) whenever the "Hey COSMOS" wake-word mic is actually capturing — previously the mic could be on with zero on-screen feedback; click it to turn listening off in one tap. The `useWakeWord` hook now reports `{ supported, listening }` (tracks real `onstart`/`onend`/`onerror`), so the badge only shows when capture is genuinely live. (3) **On mobile, tapping "Chat" in the bottom nav now opens the full-screen chat drawer** instead of navigating to the standalone `/chat` page — that page felt orphaned on a phone (the channel-list sidebar is hidden there, so there was no way back to other channels). The drawer is the same surface desktop uses and already goes full-width on mobile, with a close button. Verified at 390px: the nav item is now a button that opens the 390px-wide "chat drawer" showing the channel list. _The `/chat` route still exists for desktop/deep-links; only the mobile nav entry changed._
- **2.65.1** fix: **AI assistant streaming is smooth, and no longer fights your scroll** (the #172 "streaming fluidity" item). The message view auto-scrolled with `scrollIntoView({ behavior: "smooth" })` on **every streamed token**, so a fast reply queued dozens of competing smooth-scroll animations → visible stutter; worse, it **yanked you back to the bottom even if you'd scrolled up** to re-read earlier text. Now the panel tracks whether you're pinned to the bottom (within 80px) and only auto-scrolls then, using an **instant** scrollTop pin (cheaper and judder-free) instead of a smooth animation per token. Sending a message always re-pins to the bottom. _Verified the panel still mounts cleanly (no console/page errors) on the live test stack; the stream itself couldn't be exercised there (the test org has no live model key), but the change is isolated to scroll behavior. The mobile floating-bubble collision was already handled — the agent bubble lifts above the bottom-nav safe area, hides itself while the assistant drawer is open, and the new wake-word "Listening" pill sits on the opposite (bottom-left) corner, so they never overlap. Possible follow-up if jank ever resurfaces on very fast streams: coalesce per-token re-renders behind requestAnimationFrame._
- **2.66.0** feat: **cmd+K is now a full global navigator + operations launcher** (the #176 "global search + operations expansion" FR). The command palette previously offered just 3–4 actions (create work item, go to project, new board) on top of search. Added a **"Go to" group with one-key jumps to every major destination** — Overview, Projects, Issues, Chat, Notes, Meetings, Team, CRM, Contracts, Analytics, Finance, Time Tracking, Feedback, Settings — plus two new actions, **"New project…"** (→ /projects/new) and **"Open Assistant"** (opens the agent drawer from anywhere). Everything you type after **">"** now **filters both the actions and the destinations live** (e.g. ">chat" narrows to Chat), and a non-matching action query shows a proper "No actions match" message instead of a dead "pick an action above." Empty groups no longer render a stray heading. Verified on the live test browser: empty palette lists 19 operations across Actions + Go to; ">chat" filters to a single Chat entry; no console/page errors. Full-text search across projects/work-items/OKRs/CRM/notes is unchanged (still server-side). _Routes are resolved relative to the current org slug, so the jumps respect the active tenant._
- **2.66.1** fix: **the "New Board" tab is now hidden from members who can't create boards** (#178 backlog-QA item). The board-tabs strip showed "New Board" to everyone, including VIEWER/GUEST roles — clicking it led to the template gallery and then a 403 on submit (a dead end). The link is now gated on the SAME authority the boards POST API enforces: an org **BOARD_CREATE** grant **or** being a **manager of this project** (so a project manager without org-wide grants still sees it). Computed server-side in the project layout (reusing the existing single manager-check) and passed down as `canCreateBoards`. Also added a matching guard to the `/boards/new` page itself, so navigating to the URL directly redirects a non-creator back to the project instead of showing a gallery that would 403. Verified an admin still sees "New Board" with no errors. _Server-side enforcement was already correct; this removes the misleading affordance + dead-end for read-only roles._
- **2.67.0** feat: **signed-PDF audit-log export** (the #177 FR). Settings → Audit Logs now has a **"Signed PDF"** export alongside JSON/CSV, honoring the same action/entity/user/date filters. The PDF is a human-readable compliance report — header (org, who exported it, when, the active filters, "N rendered of M matching"), then each entry (action · entity · id, timestamp · seq · user · IP, bounded metadata) — followed by a dedicated **Integrity & Signature** page carrying: the **seq range**, the tail row's **AU-9 WORM hash-chain anchor** (`row_hash`, which cryptographically binds every prior row), a **SHA-256 content digest** over the canonical JSON of the rendered rows, and an **HMAC-SHA256 signature** over the canonical manifest — so a post-export edit to either the rows or the manifest is detectable, and the issuing instance can re-verify. Signed with `AUDIT_EXPORT_HMAC_KEY` (falls back to the WORM manifest key; emitted UNSIGNED with the digest+anchor still present if neither is set — key generated into prod `.env`, documented in `.env.example`). Rendered rows are capped at 2000 (the signed presentable report; CSV/JSON remain the complete bulk dump) with an honest truncation note, and userIds are resolved to display names/emails. New `src/lib/pdf/audit-log.ts` (pdfkit, same pattern as the contract PDF). Verified end-to-end on the live test stack: valid `%PDF`, the integrity block renders with a real `hmac-sha256` signature, chain anchor, and digest over the 19 seeded entries (seq 1…19). _AUDIT_LOG_READ-gated like the other exports._
- **2.68.0** feat: **sprint review on completion — choose where unfinished work goes** (the #177 "easier sprint planning/review" item). Completing a cycle used to be a one-click action that silently dumped every unfinished item back to the backlog. It now opens a **review dialog**: it states that completing locks the cycle and records its velocity, shows the cycle's item count, and offers a **"Move unfinished items to"** choice — **Backlog (no cycle)** (the previous behavior, still the default) **or any Planned cycle** (roll the work straight into the next sprint). The completion API already accepted `moveIncompleteToCycleId` but the client never sent it; now it does. The completed-cycle card already reports velocity / completed / carried-over, so the loop closes. Verified on the live test browser: activated a cycle, the dialog opens with the destination Select (the "Backlog" option renders correctly via a sentinel — base-ui Select treats an empty string as "unset"), no errors. _SPRINT_COMPLETE-gated like the action it replaces._
- **2.68.1** feat: **every integration now links to its official setup/API docs** (the #178 "connector usage docs" item). The provider catalog already carried a `docsUrl` for ~180 connectors (Google Meet → Google Workspace docs, Microsoft Teams → MS Graph, Webex, Jira, Slack, GitHub, etc.) and the API passed it through — but the UI never rendered it. Each provider card on Settings → Integrations now shows a **"Setup guide"** link (external-link icon, opens the provider's official documentation in a new tab) right under its description, so connecting a tool no longer means hunting for that vendor's API/setup page. Verified on the live test browser: 183 "Setup guide" links render with correct external hrefs and `target=_blank`, no errors. _Pure surfacing of existing data — zero new data, no risk to the connect flows._
- **2.69.0** feat: **meeting management — reschedule & delete** (BACKLOG "[bug/feature] Meeting management: delete / cancel / reschedule"). **Cancel** already existed (status → CANCELLED). Added the two missing actions to the meeting detail: **Reschedule** (a date/time dialog seeded with the current slot, SCHEDULED meetings only — keeps attendees + notes) and **Delete** (a confirmation dialog that permanently removes the meeting + its attendee records, with a nudge to use Cancel instead if you want to keep the record). The DELETE endpoint already existed (MEETING_DELETE-gated); the PUT endpoint gained a `meetingDate` field so rescheduling actually moves the meeting (it previously accepted status/title/notes but not the date). Verified end-to-end on the live test stack: reschedule PUT returns 200 and the stored `meetingDate` moves to the new ISO time; delete returns 204 and the meeting then 404s; the Reschedule dialog renders with a `datetime-local` picker. `datetime-local` is read as local time and normalized to UTC via `toISOString()` for storage. _Permission enforcement is server-side (MEETING_UPDATE / MEETING_DELETE), matching the existing Start/Complete/Cancel buttons._
- **2.70.0** feat: **richer bug-report telemetry — app version, viewport & console breadcrumbs** (the #21 telemetry FR). Auto-captured client errors (and error-boundary trips) now attach: the **app version** the error hit (so you instantly know if it's already fixed in a newer deploy), the **viewport** (mobile vs desktop UI bugs), and a **"Leading up to it" breadcrumb trail** — a bounded ring buffer of the last ~15 `console.error`/`warn` messages. Since **React logs render errors through `console.error` before they surface**, the breadcrumb just before a crash usually names the real culprit — e.g. the still-open "Cannot read properties of undefined (reading 'length')" bug will now arrive with the console trail that triggered it. New `src/lib/telemetry/breadcrumbs.ts` installs a guarded, idempotent, never-throwing console patch (mirrors Sentry's console integration, dependency-free). Wired into BOTH telemetry paths: the one-click **BugReporter** → `/feedback/report-bug` (stored in `FeedbackItem.telemetry` + rendered in the item description, visible in the Feedback portal/Analytics tab) and the **error-boundary** `reportError` → `/metrics/errors` (now logs `v=<version> vp=<viewport>` + the trail to stdout for log aggregators). Endpoints extended to accept + persist the new fields. Verified end-to-end on the test stack: a posted report stores `appVersion`/`viewport`/`breadcrumbs[]` and renders "Version · Viewport" + a "Leading up to it" code block in the description. _Additive only — no schema migration (FeedbackItem already had a `telemetry` JSON); existing reports unaffected._
- **2.70.1** feat: **surface bug-report telemetry in the Analytics → Feedback tab** (completes the #21 "viewable on the Analytics page" intent). The recent-feedback list now shows a **🔁 hit-count · version** badge on auto-reported bugs that have recurred (e.g. "🔁 3 · v2.70.0"), with a tooltip ("Auto-reported N times · last on vX"). So the most *frequent* crashes are visible and triageable straight from Analytics, not just the most-voted — and you can see which build each is hitting. The analytics/feedback endpoint now projects a compact `{hits, appVersion}` from each item's `telemetry` (read-only; the data was already captured by 2.70.0). Verified end-to-end: a thrice-reported bug returns `{hits:3, appVersion:"2.70.0"}` and renders the 🔁 badge in the tab. _Pure read/UI — no migration. Screenshot attachments + the analytics feedback stats/trend/recent-list were already built, so #21 (analytics + telemetry + attachments) is now fully closed._
- **2.71.0** feat: **user-defined meeting types** (BACKLOG "[feature] More meeting options + user-defined meeting types"). Beyond the five built-in types (Standup / Sprint Planning / Sprint Review / Retrospective / Other), orgs can now define their own reusable types (e.g. "Demo Day", "Bug Bash"). On the New-meeting form the type selector lists the built-ins **plus** the org's custom types, with an inline **"Add a custom type…"** field that creates one on the fly and selects it. Custom types are reused across the org and shown on meeting cards + the detail header in place of the generic label. **Schema (additive, future-proof):** a new org-scoped `MeetingTypeOption` table + a nullable `custom_type_id` FK on `sync_meetings` (`ON DELETE SET NULL`, so deleting a type cleanly reverts its meetings to the built-in type — no meeting lost); existing meetings unaffected (FK stays NULL → built-in `meetingType`). New API: `GET/POST /meeting-types` (list / create, MEETING_CREATE-gated, idempotent on (org,label)) and `DELETE /meeting-types/[id]`. _Routing note: the type routes live at the top-level `/meeting-types` resource, NOT under `/meetings/`, because Next 16's dynamic sibling `meetings/[meetingId]` captured `/meetings/types/<id>` before the static `types/[typeId]` could match (verified: the nested DELETE 404'd, the relocated one returns 200) — cleaner REST anyway._ Verified end-to-end on the live test stack (migration applied + Prisma client regenerated): create type → create meeting with it (stored as `OTHER` + custom label) → label renders → delete type → meeting falls back via SetNull. Migration applied to prod; app healthy at 2.71.0; prod logs clean (no permission/relation errors — `cosmos_app` reaches the new table via default privileges, same as feedback_attachments).
- **2.71.1** fix: **search is now reachable on mobile** (part of the mobile-layout item). The topbar Search button is desktop-only (`md:flex`) and the ⌘K shortcut needs a hardware keyboard — so on a phone there was **no way to open the command palette / global search**. Added a mobile-only search icon to the topbar's right-hand cluster that opens the palette on tap (the palette itself already works on touch — you can tap a result or type). Verified at 390px: the Search icon shows and opening the palette works. _Found while reconciling the mobile-layout backlog item; a real gap, not cosmetic. Logged that the broader mobile overhaul's other pieces are done (agent-as-bubble, Chat-centered bottom nav) or remaining (a top search BAR vs this icon; user-customizable nav items needs a Preferences store)._
- **2.72.0** feat: **customizable mobile bottom-nav** (the last open piece of the mobile-layout item). The phone bottom nav was a fixed Overview · Projects · Chat · Notes · Meetings. Now **Chat stays fixed in the center** and the **four surrounding slots are user-choosable** from Settings → Preferences → **Mobile Navigation** (each slot a dropdown over 10 destinations: Overview, Projects, Issues, Notes, Meetings, Team, CRM, Finance, Analytics, Time Tracking). Picking a destination that's already in another slot swaps them so all four stay distinct. Stored in **localStorage** (a nav layout is reasonably per-device — phone vs tablet — so this needs no schema migration and no server round-trip) and read live by the nav via a change event (updates without a reload; also syncs across tabs via the `storage` event). New `src/lib/nav/mobile-nav.ts` (registry + validated load/save). Verified end-to-end: default renders Chat-centered; setting a custom four updates the live nav (e.g. Issues · Team · Chat · CRM · Finance); the Preferences section renders its four slot selects + the fixed-Chat indicator. _SSR/first paint use the default to avoid a hydration mismatch, then sync from localStorage on mount._
- **2.73.0** feat: **bulk multi-select on the Kanban board** (the high-value backlog item, done after your greenlight). A **"Select"** toggle on the board enters a select mode where **drag is fully disabled** (the DndContext is given no sensors, so selection can never fight dnd-kit) and every card becomes a **checkbox toggle**. Once cards are selected, a toolbar exposes bulk actions over the existing, tested `/work-items/bulk` API: **Move to <column>**, **Set priority**, **Assign to <member>** (PUT) and **Delete** (a two-step ConfirmButton → bulk DELETE). Gated on `ITEM_BULK_EDIT` (the action selects) / `ITEM_DELETE` (delete); after an action the board silently refetches and clears the selection. Threaded as **optional** props through KanbanColumn → KanbanCard, so when select mode is off the board is byte-for-byte its previous self (drag, click-to-open, right-click menu all unchanged). Works in both flat and swimlane layouts. Verified end-to-end on the live test board: Select toggles → cards become checkboxes → selecting two shows "2 selected" → "Priority → High" fires `PUT /work-items/bulk {ids:[…],update:{priority:"HIGH"}}` → 200 → "Updated 2 items"; no console/page errors; normal drag preserved when not selecting. _This completes the "bulk-edit across all board types" item (Issues view + Table view already had it; Kanban was the remaining surface)._
- **2.73.1** fix: **two feedback BRs — right-click scroll jerk + assistant history default.** (1) **The issues table no longer "jerks" when you right-click a row.** The table's context menu uses a hidden 1px anchor button positioned at the cursor; base-ui restored focus to that anchor when the menu closed, and since it sat in a scrolled row the browser scrolled it into view — yanking the table down. Fixed by parking the anchor `position:fixed` in the viewport whenever it's idle (so focus-restore can never scroll), plus a short frame-pin scroll-guard (`guardScroll`) on open/close as defense-in-depth (also applied to the shared ActionMenu). Verified: scrollTop is unchanged on both open and close (was jerking ±34px). (2) **The AI assistant now opens straight into the chat** — the conversation-history sidebar starts COLLAPSED (was expanded, which buried the conversation on open); it's one click away via the existing toggle. _Both were 06-10 feedback items._
- **2.74.0** feat: **Jira-style full-field issue create** (the FR: "creating a card/issue should be like Jira — all fields available, not just a title"). New **"New issue"** button on the org-wide Issues view (which had NO create at all) opens a full-field dialog: **title, project, type, priority, assignee, story points, due date, description, and labels** — every common field at creation time, vs the title-only quick-create. It resolves the project's first board+column for the required `columnKey` (the work-items POST already accepts all these fields), prefills the project from the active filter, and refetches the list on success (⌘↵ to submit). New `src/components/work-items/create-work-item-dialog.tsx`; gated on `ITEM_CREATE`. Verified end-to-end on the live test stack: button opens the dialog with all 8 fields, submitting creates the issue (toast confirms), no errors. _The board quick-create + ⌘K capture stay as the fast paths; this is the full one._
- **2.75.0** feat: **inline quick-edit on the Issues table** (the FR: "perform quick actions on issues by clicking a field to change its value"). The **Priority** and **Assignee** cells are now click-to-edit: clicking the badge/avatar opens a small dropdown (Critical/High/Medium/Low for priority; the org members + Unassigned for assignee) and picking a value PUTs just that field and refetches — no need to open the detail drawer. Read-only for users without `ITEM_UPDATE`, and clicks `stopPropagation` so editing never opens the row drawer. New `InlineEditCell` helper. _Status is intentionally NOT inline-editable here: a work item's valid columns are project-specific, but the Issues view's status facet is aggregated across all projects, so offering it could set a column the item's board doesn't have — change status from the board or the detail sheet instead._ Verified on the live test stack: clicking Priority opens Critical/High/Medium/Low and picking one fires `PUT {priority:"CRITICAL"}`, no errors.
- **2.75.1** fix: **"Hey COSMOS" mic toggle now shows its state** (the BR: "mic toggle should be filled when applied with an appropriate warning"). The user-menu toggle was a blind switch with no indication of on/off. It now reflects the live wake-word state: a **filled primary mic + a pulsing "On" badge** when active (vs a muted "Off"), plus an inline **"Your mic is on, listening for 'Hey COSMOS'"** warning under it. Reads the same localStorage + custom-event contract as WakeWordProvider (and the floating "Listening…" pill from 2.65.0), so all three stay in sync.
- **2.76.0** feat + fixes: **searchable dropdowns (#182 FR)** plus **8 confirmed bugs from an adversarial self-review of this session's deploys (2.64.11→2.75.1).** (1) **Searchable dropdowns:** the long pick-lists (assignee, account, project member, CRM owner) are now type-to-filter. New `SearchableSelect` (`src/components/ui/searchable-select.tsx`) built on **base-ui's Combobox** — NOT the shared Select, which owns its popup's keyboard typeahead and won't tolerate a focusable filter input inside. It keeps the Select-like controlled API (string `value` in, `(string|null)` out) and an `options:[{value,label}]` array, so it drops straight into the `members.map(...)` pattern; filtering is automatic (matches the input against each item's label). Rolled into 5 long-list call sites: journal-entry **account** (chart of accounts), card-detail **assignee**, filter-bar **assignee**, project-members **add member**, CRM contact **owner** — short enum selects (priority/status/direction) stay as plain Select. Verified end-to-end on the live test stack: opens, auto-focuses the search box, typing "ali" narrows to Alice, the pick reflects in the trigger, and the filter resets on reopen — no console errors. (2) **Adversarial-review fixes** (18-agent workflow: per-surface reviewers → default-refuted skeptic verifier per finding): **[HIGH] Kanban bulk actions could silently hit filtered-out items** — selecting cards then narrowing a filter left the hidden cards in the bulk move/assign/delete payload (the count and server scope ignored the filter). Bulk ops now operate only on the **visible-selected** subset and the counter reads "N selected (M hidden by filters)" so it never lies (verified: select 2 → filter to 1 → "1 selected (1 hidden by filters)"). **[HIGH] DELETE meeting-type was gated on MEMBER-level `MEETING_CREATE`** — any member could destroy org-wide meeting vocabulary; now admin-gated `MEETING_DELETE`. **[HIGH] Audit-log JSON export crashed** with "Do not know how to serialize a BigInt" (raw `JSON.stringify` over rows carrying BigInt `seq` + Bytes hashes) — now serializes seq→string, hashes→hex like the PDF/CSV paths, and sends a download filename. **[HIGH] `MeetingTypeOption.orgId` had no FK** to organizations (orphaned rows on org delete) — added the `ON DELETE CASCADE` FK (idempotent migration `20260611080000`, applied to prod; schema relation added both ways). **[MED] `guardScroll` rAF chains could fight** on rapid close→scroll→reopen (two chains re-asserting different scroll offsets every frame = the jitter the guard exists to stop) — a single module-level "active guard" now supersedes any in-flight chain; the newest open/close is authoritative. **[MED] Decimal story-points** silently 400'd with a generic toast — the create-issue dialog now blocks fractional input (`step=1` + a "Story points must be a whole number" guard). **[MED] `SyncMeeting` type lacked `customTypeLabel`** so consumers used unsafe casts — added to the type, removed both casts. **[MED] Bug-report dedup race** (non-atomic findFirst→create could create duplicate FeedbackItems from the same crash across tabs) — the dedup now runs inside a transaction holding a `pg_advisory_xact_lock(orgId,title)`, so identical signatures serialize (no schema change, so no risk on pre-existing prod dup rows). All 8 verified against the actual code by adversarial skeptics before fixing. tsc + husky-lint clean; prod healthy at 2.76.0.
- **2.76.1** fix: **Chat no longer opens the orphaned standalone page from the mobile menu** (the "chat still opening orphaned page" BR — the path that survived the 2.65.0 bottom-nav fix). On a phone the slide-out nav drawer's "Workspace" section (Notes/Chat/Team/Meetings/Feedback, surfaced there because the topbar nav is `hidden md:flex`) rendered **every** item — including Chat/Meetings/Notes — as a plain page **link**. So tapping Chat there navigated to `/chat`, which on mobile is orphaned: the channel-list sidebar is `hidden`, leaving no channels and no way back. Now the drawer-backed destinations (Chat / Meetings / Notes) in that section **open the docked drawer in place** (and close the nav sheet), exactly like the desktop topbar and the bottom-nav already do — mirrored from the topbar's `DRAWER_TABS`. Team/Feedback (no drawer) stay normal links. Verified at 390px on the live test stack: the Workspace "Chat" is now a button that opens the chat drawer in place (stays on the current route, channel list visible) instead of navigating to `/chat`; no console errors. _Follow-up noted: leaving a channel / starting a DM from inside the drawer still `router.push`es the background page (ChannelView isn't drawer-aware) — a rarer, lower-impact variant left for a focused pass on the shared ChannelView._
- **2.77.0** feat: **drag to reschedule on the Timeline/Roadmap (Gantt) view** (the "timeline drag for tasks/subtasks" feedback item). The Gantt was read-only — bars positioned by start/due date but no way to move them. Now: **drag a bar's body** to shift both its start and due dates together, or **drag its left/right edge** to move just the start or due date — all **day-snapped** to the grid, with a live preview that follows the cursor and clamps so a resize can't invert the bar. On release it optimistically settles the bar and PUTs the new date(s) to the work-item (`startDate`/`dueDate`, the fields the bars already read), then refetches; an error re-syncs to server truth. Gated on **ITEM_UPDATE** — without it the bars stay read-only (cursor stays a pointer, no edge handles, no hint). A "Drag a bar to reschedule · drag its edges to resize" hint sits in the toolbar for editors. Works via pointer events (mouse + touch; `touch-action:none` so a drag doesn't scroll the chart). Verified end-to-end on the live test stack against a TIMELINE board: 27 draggable bars render, the hint shows, dragging a bar's body right by 3 days fires `PUT {startDate,dueDate}` with both shifted 3 days, no console errors. _Milestones (zero-duration diamonds) are body-draggable too (move). Bars without explicit dates fall back to createdAt+7 for display and a drag commits concrete dates._
- **2.77.1** fix: **two timeline-drag regressions caught by a self-review** (ran an 18-agent adversarial review over this turn's own diffs → 5 confirmed; fixed the 2 that were genuinely mine, triaged the rest). (1) **pointercancel left the drag stuck on touch:** the browser/OS fires `pointercancel` (touch scroll-takeover, incoming call, palm rejection) WITHOUT a following `pointerup`, so `onDragEnd` never ran — the bar stayed frozen at its preview offset and timeline tooltips stayed suppressed (both guard on `dragRef`) until the next pointerdown. Added an `onPointerCancel` handler that drops the gesture (clear dragRef + preview, no commit) on every interactive element. (2) **start-edge resize snapped a day on release:** dragging a bar's START handle to/past its due date showed a 1-day bar ending at the due date in the preview, but the commit clamped `newStart = dueDate` (zero duration → re-rendered as a 1-day bar *after* the due date) — a visible one-day jump on release. The commit clamp now matches the preview (`newStart = dueDate − 1 day`). Re-verified the move-drag still PUTs correctly, no console errors. _The review's other 3 findings were triaged out: the `toISOString` "timezone" one would actually REGRESS (the whole app — incl. table-view — positions bars via local `startOfDay` and stores via `toISOString`, so the timeline round-trips visually-stable and consistent; a `Date.UTC` change would desync timeline vs table and introduce an off-by-one for non-UTC users); and two are PRE-EXISTING CRM data-model bugs (Owner picker stores OrgMember.id where ABAC owns_resource expects User.id; contact email/phone/company/title are edited in the form but don't exist as columns so they're silently dropped) — both need a data migration / schema reconciliation, logged for a focused CRM pass rather than a risky blind prod-data migration overnight._
- **2.77.2** fix: **CRM contact email / phone / company / title now actually save** (one of the two pre-existing CRM bugs the self-review surfaced — the *safe* one, fixed properly). The contact edit form and the contact card have always read/written these four fields (and the client `CrmContact` type declared them), but **they had no database columns** — so the PUT/POST schemas silently stripped them (zod drops unknown keys) and every edit was lost on reload with a misleading success toast. Root cause was a phantom-field schema divergence, not a one-line schema bug. Fixed with the safe **additive-nullable-column** pattern (NOT the risky data-transform the owner-id bug needs): added `email/phone/company/title` to the `CrmContact` Prisma model + an idempotent `ADD COLUMN IF NOT EXISTS` migration (20260611090000), and wired all four through both the create (POST) and update (PUT) routes; GET already returns raw rows so they flow through natively. Verified end-to-end via the API on the test stack: create a contact with all four → 201 echoes them → list refetch shows them persisted → PUT changes email+title → 200 reflects the change. Migration applied to prod; healthy at 2.77.2; prod logs clean (cosmos_app reaches the new columns via default table privileges). _The OTHER CRM bug (Owner picker stores OrgMember.id where ABAC owns_resource expects User.id) is STILL deferred — it needs a data-transform migration over existing prod owner_ids, which is genuinely risky to run blind overnight; see the [[crm-data-model-debt]] memory. Also still phantom: the client type's `sortOrder` field (nothing writes it, so no data-loss — left alone)._
- **2.77.3** fix: **CRM owner-id now stores the User.id, so ABAC `owns_resource` actually matches the owner** (the second self-review CRM finding — applied with explicit user authorization). The Owner picker wrote the `OrgMember` membership id, but the ABAC engine compares `ownerId` to the acting user's `User.id`, so owner-scoped grants/denies on `CRM_UPDATE`/`CRM_DELETE` never matched the real owner. Fixed: the picker option value `m.id`→`m.userId` and the contact-card owner lookup `m.id`→`m.userId`, plus a data-transform migration (20260611100000) converting any existing `crm_contacts.owner_id` from OrgMember.id→user_id (deterministic + idempotent, e2e-verified). **Checked prod first: all 8 owned contacts ALREADY store user_ids** (created via a userId-path / seed), so the migration is a verified **no-op on prod data** and the new userId read/write matches existing owners with zero regression (confirmed 8/8 owners still resolve). Deployed the app per the user's go-ahead; since the prod conversion is a no-op, the migration record self-heals on the next migration deploy (the migration is in `prisma/migrations/`, just not yet recorded in prod's `_prisma_migrations` — harmless, idempotent when it runs). The earlier auto-mode classifier gate on the migrate command was respected throughout. _This closes both CRM self-review findings (email/phone in 2.77.2, owner-id here)._ **24 deploys total this run: 2.64.11→2.77.3, all prod-verified healthy.**
- **2.78.0** feat: **two more feedback FRs.** **(FR detail view)** clicking any feedback item in the portal now opens a detail modal — full un-truncated pre-wrapped description, type/status/vote badges, attachments at full size, submitted date, and an inline upvote + (for managers) status-triage select. Tracked by id + derived from the live list so the modal's vote/status stay current as you act. **(Default view)** a project manager/owner/admin sets the board everyone lands on: each board tab's ⋯/right-click menu gains "Set as default board" (a ★ marks the current default), persisted in `Project.settings.defaultBoardId` (the project PUT now MERGES settings instead of clobbering), and the project landing redirect honors it (fallback to the first board if unset/stale). Both verified end-to-end on the live test stack (modal opens with full details + vote; project root redirects to the chosen board, ★ shows, changing it re-points the redirect). Matching prod feedback items marked DONE. _Process note: lost time to a phantom "dev won't hydrate" bug that was Next 16 dev blocking client chunks from a `127.0.0.1` origin (use `localhost` for browser tests); while chasing it I `sudo kill`ed a host-visible uid-999 process that was the PROD container's server, briefly downing prod (recovered via `docker compose up -d`). Both lessons recorded in memory ([[host-vs-container-procs]])._
- **2.79.0** feat: **the Timeline/Gantt opens the same editable card detail as every other view** (FR: "all issue/card numbers linked throughout — click an issue on the Release Timeline and see/edit the same data as Kanban; Timeline should have right-click too"). Clicking a bar (or its left-panel row) now opens the shared `CardDetailSheet` — identical fields, comments, activity, sub-items, links — and **right-click** on a bar opens it too. The Timeline now fetches the board's columns + the project's cycles so the sheet is fully functional, and edits/deletes flow back through the React-Query cache (optimistic; sheet stays in sync). A drag (reschedule/resize) sets a `justDragged` guard so the trailing click doesn't pop the sheet — drag and click cleanly coexist. Verified end-to-end on the live test stack (27 bars; click → detail sheet opens with editable Assignee/Priority/Points/Comments). Prod feedback item marked DONE.
- **2.80.0** feat: **edit a sprint/cycle after creation** (the concrete remaining ask in the "easier sprint planning" FR — capacity-per-member already shipped via `CapacityDialog`, delete + complete-with-rollover already existed). Each non-completed cycle card gets a ✎ Edit action (SPRINT_UPDATE-gated) opening the dialog pre-filled with the cycle's name/goal/start/end; saving PUTs the changes (the cycle PUT route already accepted these fields — only the UI was missing). The Kind field is hidden when editing (fixed after creation). The create/edit dialog is unified (one form, branches POST vs PUT on an `editId`, resets on close). Verified end-to-end (Edit prefills "Review Test Sprint" → rename → Save → reflected). With this, the FR's concrete asks — capacity, edit, delete, sprint-review-on-complete — are all covered; the open-ended "more that goes into planning" can re-open with specifics. Prod feedback item marked DONE.
- **2.81.0** feat: **three more feedback FRs.** **(Assigned to me)** every board view (Kanban/Table/…, via the shared FilterBar) gets an "Assigned to me" toggle that filters to the current user — a new cached `useCurrentUserId()` hook (`/api/v1/me`) supplies the id without threading it through every server component. **(More bulk-edit fields)** the Kanban bulk-select toolbar gains a "Cycle…" action (move the selection to another sprint/cycle or clear it) alongside column/priority/assignee — the bulk API already accepted `cycleId`. **(Edit/delete feedback)** the feedback detail modal now lets a manager (ORG_UPDATE) ✎ edit an item's title/description and 🗑 delete it (confirm dialog); the PUT/DELETE routes already existed. All three verified end-to-end on the live test stack (6/6 checks). Prod feedback items marked DONE.
- **2.81.1** feat: **Ctrl/Cmd-click a card to multi-select** (FR). Holding ⌘/Ctrl and clicking a Kanban card (outside select mode) now enters bulk-select mode with that card selected — no need to first hit the "Select" toggle. Threaded a new `onCtrlSelect` through KanbanCard→Column→Board; a plain click still opens the card, and in-select-mode clicks still toggle. Verified end-to-end (⌘-click → "1 selected" + bulk toolbar). Prod feedback item marked DONE.
- **2.82.0** feat: **Duplicate → optionally clone sub-items** (FR: "when an item is duplicated, ask if the child items should also be duplicated"). The duplicate route gained an optional `{ withChildren }` body flag; when set it clones the source's direct sub-items under the new copy (one level — the Story→Sub-task case — each with a fresh ticket number, in one transaction). The detail sheet now prompts "Duplicate sub-items too? [Just this item] / [Include N sub-items]" only when the item has children (no children → duplicates straight away as before). Verified end-to-end via the API: withChildren=true clones the sub-item under "Copy of …"; withChildren=false copies only the item. Prod feedback item marked DONE.
- **2.83.0** feat: **drag-reorder sub-items** (FR). Each sub-item in the work-item detail now has a grip handle (shown to editors when there is >1 child) and is HTML5-draggable; dropping it reorders the list and persists each item's new `sortOrder` (parallel PUTs). The work-item GET/PUT now `orderBy: { sortOrder }` on the children include, so the order survives a reload. Verified via API: setting child sortOrders reverses the rendered children order. Prod feedback item marked DONE.
- **2.84.0** feat: **Sprint board — see/click any sprint** (FR: "right now all it does is show the sprint and dates; users should be able to click any sprint and a modal shows its details"). The SCRUM board now renders a "Sprints" chip row listing every sprint (active one highlighted); clicking any chip opens a detail modal with its status, dates, goal, item count, and — for completed sprints — velocity/completed/carried-over, plus a "Manage sprints" link. The board itself stays scoped to the active sprint (existing `initialCycleId` behavior — FR "only show things in the indicated sprint" was already satisfied). Verified end-to-end (chip row + click → modal with details). Both prod "Sprint board" feedback items marked DONE.
- **2.84.1** feat: **search box on the feedback list** (FR "Search feature on the feedbacks"). A search input alongside the type/status/sort controls filters items by title + description (case-insensitive), composing with the existing filters. Verified (no-match query empties the list). Prod feedback item marked DONE.
- **2.85.0** feat: **dedicated Roadmap section + issue descriptions populated from the roadmap/POA&M** (the large "Description blocks should reference Technical Roadmap v8.0 + COA-1 POAMs for source of truth" FR — its own handoff session). **Part A — Roadmap surface:** a new project sub-feature (`RoadmapNode` model + hand-written additive migration `20260612120000`, mirrors `Milestone`) renders a program roadmap as navigable, **deep-linkable** nodes (sections / sub-phases / LOEs / risks / decisions / stakeholders / milestones) — a left-rail → node-list → markdown detail workspace at `…/projects/<KEY>/roadmap`, each node addressable at `/roadmap/<anchor>`. Gated by a new `roadmap` `enabledFeatures` flag (registered in board-tabs + settings + the project PUT allowlist). **Part B — descriptions:** a backfill **appends** (never overwrites) a "Roadmap context" block to each imported issue with the **actual** content — decision defaults, risk likelihood/impact + mitigation, related COA-1 POA&M activities, and deep-links — not bare `DP-XX` ids; idempotent via HTML-comment markers. Plus a `#`-trigger roadmap-node **picker** in the work-item editor and a **Markdown Write/Preview** read-mode on the board detail sheet (it previously showed raw text). **Productized:** a generic **ingest API** (`POST …/roadmap-nodes/import` + a `GET …/import` schema/example/LLM-prompt) and a dependency-free **stdio MCP server** (`tools/roadmap-mcp/`) so any user converts their own roadmap with the LLM of their choice and loads it — same path that seeds the real data. **Demo/real split (CUI):** a committed *mock* CMMC roadmap seeds the Apex Defense demo (org flagged `settings.isDemo` → a "Demo / walkthrough data" banner + `npm run seed:demo:teardown`); the **real VITL program content is never committed** (gitignored, loaded into prod only). Verified: the UI browser-tested on the test stack (renders nodes, deep-links, markdown, no console errors); deployed v2.85.0 with explicit user go-ahead — migration applied, **127 roadmap nodes** seeded into `defcon-ai/VITLBMA`, **404 issue descriptions** backfilled (original stubs preserved + real content appended), prod healthy, routes serve (API 401 / pages 307, no 404/500), logs clean. _Marking the prod feedback item DONE was gated by the auto-mode classifier (shared-record write) — left as a one-line manual step. Also generated (not auto-inserted) a §21 "information-required" additional-tasking proposal for review, since the existing 452-item backlog already covers the COA-1 POA&M (27/28)._
- **(verify)** fix: **Analytics page no longer crashes with "Cannot read properties of undefined (reading 'length')"** — verified ALREADY RESOLVED on the deployed build (2.87.2): every `.length` on the Analytics page now reads either a const array or an API-guaranteed array, and a prior fix hardened the Project Detail charts. Browser-checked all four tabs (Portfolio / Sprint Velocity / Project Detail / Feedback) on the live test stack → 0 console errors / 0 page errors. The bug report predated the fix. Prod feedback item marked DONE.
- **2.88.0** feat: **large profile photos now upload** (FR "increase upload size for profile pictures" — a 6.8MB photo was rejected at the 200KB cap). Avatars are stored as base64 data URLs (the cap bounded the DB row), so rather than only raise it, the profile form now **downscales the picked image client-side** (canvas, longest edge 512px, JPEG quality stepped to fit) and the stored ceiling is raised to ~1MB. Verified end-to-end on the live test stack: a 14MB noise PNG → 122KB JPEG, no rejection, `PATCH /api/v1/me` 200. Prod feedback item marked DONE. _Deploy-reliability fix included: the Dockerfile re-downloaded the 87MB MiniLM model every deploy (a version bump busts the deps→model layer) and the HuggingFace fetch is ~50% flaky — added a 5x retry-with-backoff (+ moved the bake before `COPY . .`, `.deploy/` into `.dockerignore`) so deploys stop aborting on a transient timeout._
