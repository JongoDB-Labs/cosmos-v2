apiVersion: v2
name: cosmos
description: COSMOS-v2 — UDS-native core stack (app, CrunchyData PGO Postgres, MinIO, migrate hook, UDS Package)
type: application
# Chart version (bump on chart changes) vs appVersion (the cosmos-v2 release we deploy)
version: 0.1.0
appVersion: "2.102.2"
# Operators (PGO, MinIO) are installed cluster-wide out-of-chart; this chart ships the
# instances (PostgresCluster, Tenant) + the app + the UDS Package CR. No chart deps.
